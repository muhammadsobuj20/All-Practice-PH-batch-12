
const numbers= [1,2,3,4,54];

const reversed =[];
for(let i = 0; i < numbers.length; i++){
    // console.log(numbers[i]);
    // reversed.push(numbers[i]);
}
// console.log(reversed);

for(let i = 0; i < numbers.length; i++){
    // console.log(numbers[i]);
    // reversed.unshift(numbers[i]);
}
// console.log(reversed);

for( let i = 0; i< numbers.length; i++){
  
    // console.log(numbers[i]);
}
// reversed.reverse()
// console.log(reversed);
let n= 0;
while(n < numbers.length){
    // reversed.unshift(numbers[n])
    console.log(numbers[n]);
    n++;
}
// console.log(reversed);
reversed.reverse(numbers[n]);
// console.log(reversed);


for(const nums of numbers){
    // console.log(nums);
    // reversed.unshift(nums)
}

numbers.reverse();
console.log(numbers);//change kora 
// console.log(reversed);